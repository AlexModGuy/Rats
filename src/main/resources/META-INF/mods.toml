modLoader="javafml" #mandatory
loaderVersion="[28,)" #mandatory (26 is current forge version)
issueTrackerURL="https://github.com/Alex-the-666/Rats/issues" #optional
[[mods]] #mandatory
modId="rats" #mandatory
version="${file.jarVersion}" #mandatory
displayName="Rats" #mandatory
updateJSONURL="https://www.curseforge.com/minecraft/mc-mods/rats/files" #optional
displayURL="https://www.curseforge.com/minecraft/mc-mods/rats" #optional
logoFile="rats_logo.png" #optional
authors="Alexthe666" #optional
description='''
Rats.
'''
[[dependencies.rats]] #optional
    # the modid of the dependency
    modId="citadel" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[1.0.0,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.rats]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[28,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.rats]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.14.4]"
    ordering="NONE"
    side="BOTH"
